<Window x:Class="IbkrToSru.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IbkrToSru"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*"  />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Personnummer" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PersonNumber}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Växelkurs" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ExchangeRate}" />
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Csv" />
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CsvFile}" />
    </Grid>
</Window>
